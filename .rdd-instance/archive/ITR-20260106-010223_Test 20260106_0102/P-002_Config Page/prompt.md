Create in Web UI a new top menu tab named "Config" and move there the git-enabled flag and also create a switcher to turn it on and off - which to be reflected as writeback to `.rdd-instance/config/instance-config.json`