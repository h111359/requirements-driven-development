## Instructions

1. Run `.rdd/src/actions/files_list_csv_refresh.py`
   
2. For the files in `.rdd-instance/specifications/files-list.csv` which have no Description fulfilled - open the file, read it and add a short descriptin (one line, no new lines symbols) executing the script `.rdd/src/actions/files_list_csv_set_description.py` (never change the file directly).

3.  Based on the information in `.rdd-instance/specifications/files-list.csv` update the content of `.rdd-instance/specifications/files-and-folders.md` following the `.rdd/conventions/files-and-folders.convention.md` convention. 